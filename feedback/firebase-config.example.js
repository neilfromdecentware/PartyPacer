// Copy this file to `firebase-config.js` (gitignored) and fill in your own
// Firebase project's web config. Find it in the Firebase console:
//   Project settings → General → Your apps → Web app → SDK setup and config.
//
// These values are not secret (they're served in the deployed page; Firebase
// web API keys are public by design and secured by Firestore rules) — they're
// kept out of the repo only so the project is cleanly forkable.
window.__FIREBASE_CONFIG__ = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT.firebasestorage.app',
  messagingSenderId: 'YOUR_MESSAGING_SENDER_ID',
  appId: 'YOUR_APP_ID',
};
